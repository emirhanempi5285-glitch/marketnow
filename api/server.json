{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-07-09/server.schema.json",
  "name": "com.marketnow/skills-marketplace",
  "description": "Search and discover 13,859 MCP-compatible skills with Sentinel security verification. The largest open MCP skills registry.",
  "version": "3.1.0",
  "vendor": "MarketNow",
  "homepage": "https://marketnow.site",
  "icon": "https://marketnow.site/badge/marketnow-search.svg",
  "license": "MIT",
  "categories": ["search", "discovery", "registry", "security"],
  "tags": ["mcp-skills", "sentinel", "marketplace", "directory"],
  "remotes": [
    {
      "type": "sse",
      "url": "https://marketnow.site/mcp/sse",
      "description": "MCP protocol SSE endpoint for skills search and discovery"
    }
  ],
  "tools": [
    {
      "name": "search_skills",
      "description": "Full-text search across 13,859 MCP skills with category and language filters",
      "inputSchema": {
        "type": "object",
        "properties": {
          "q": { "type": "string", "description": "Free-text search query" },
          "cat": { "type": "string", "description": "Category filter" },
          "lang": { "type": "string", "description": "Programming language filter" },
          "limit": { "type": "integer", "description": "Max results (default 50, max 1000)" }
        }
      }
    },
    {
      "name": "get_skill",
      "description": "Get detailed info about a specific MCP skill by slug",
      "inputSchema": {
        "type": "object",
        "properties": {
          "slug": { "type": "string", "description": "Skill slug identifier" }
        },
        "required": ["slug"]
      }
    },
    {
      "name": "list_categories",
      "description": "List all available skill categories with counts",
      "inputSchema": {
        "type": "object",
        "properties": {}
      }
    }
  ]
}
